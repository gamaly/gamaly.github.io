# Static site, no build step — nothing generated to ignore.

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Local scratch: design preview harnesses and experiments.
# Everything in the repo root is published, so keep drafts out.
preview.html
*.local.html
